export interface ITheme{
  colors: {
    primary: string,
    secondary: string,
    secondaryShades: string,
    secondaryTins: string,
    white: string,
    black: string,
  }
  fonts: {
    Roboto: string,
    Jost: string,
    Signika: string
  };
  fontWeight: {};
  zIndex: {};
}