import styled from 'styled-components/native';
import {css} from 'styled-components';
import {CustomInput} from '../CustomInput/styled';

export const SubTaskInfoWrapper = styled.View`
  flex-direction: row;
  align-items: center;
  gap: 5px;
`;
export const StyledInput = styled(CustomInput)``;
